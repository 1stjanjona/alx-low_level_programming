#include "main.h"

/**
 * _atoi - check the string
 *
 * @s: 'the string to be converted to an integer'
 *
 * Return: return (integer)
*/
int _atoi(char *s)
{
	int i = 0;
	int sign = 1;
	unsigned int intgr = 0;

	return (intgr);
}
